
<%= form_for [@ticket, @entry] do |form|%>

	<%= form.text_field :ticket_id %>
	<%= form.fields_for :entry_selections do |selection| %>
		<%# selection.select :course_date_id, options_for_select(@course_dates) %>
		<%= selection.select :course_date_id, options_from_collection_for_select(@course_datess, :id, :start_date) %>
	<% end %>
	<%= form.submit %>
<% end %>
