<%= form_for @course do |f| %>
  <% if @course.errors.any? %>
    <div class="error_messages">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this survey from being saved:</h2>
      <ul>
      <% @course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <%= f.fields_for :course_dates do |builder| %>
    <%= render 'question_fields', f: builder %>
  <% end %>
  <%# link_to_add_fields "Add Question", f, :course_dates %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>